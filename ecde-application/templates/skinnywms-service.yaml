apiVersion: v1
kind: Service
metadata:
  name: ecde-wms
spec:
  selector:
    component: skinnywms
  type: ClusterIP
  ports:
    - name: wms-port
      port: {{ .Values.skinnyWms.port }}
      targetPort: {{ .Values.skinnyWms.port }}
